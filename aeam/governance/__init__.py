"""
aeam.governance

Enforcement of human governance over autonomous action (Phase E9).

Distinct from ``aeam.intelligence`` (which produces advisory evidence) and
from ``aeam.security`` (which authenticates and authorises the caller): this
package decides whether an already-planned, already-authorised action is
actually allowed to run yet, given what humans have and have not approved.
"""
